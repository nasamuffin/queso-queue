#pragma once

class Timer {
  public:
    void Start();
    void Pause();
    void Reset();

  private:
    // std::string _outOfTimeMessage;
    // Chat _chat;
    //std::function<...> _callback;
};
